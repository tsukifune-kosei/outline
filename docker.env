# Required
NODE_ENV=production
UTILS_SECRET=47300020c55e62eff2ea15ba35684999f83f4df03c393c94c80a2891a465485f
SECRET_KEY=47300020c55e62eff2ea15ba35684999f83f4df03c393c94c80a2891a465485f

# Database
DATABASE_URL=postgres://user:pass@postgres:5432/outline
DATABASE_URL_TEST=postgres://user:pass@postgres:5432/outline_test
PGSSLMODE=disable

# Redis
REDIS_URL=redis://:your-redis-password@redis:6379

# URL (update this to your domain or localhost with port)
URL=http://localhost:3000
PORT=3000

# Email (required for user invites, using mailpit for local development)
SMTP_FROM_EMAIL=outline@example.com
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_TLS_CIPHERS=SSLv3

# Authentication (using local authentication for simplicity, you can add other providers later)
FORCE_HTTPS=false

# Storage (using local storage for development)
STORAGE_TYPE=local
STORAGE_LOCAL_ROOT_DIR=/var/lib/outline/data

# Optional: Enable debug logging
DEBUG=*
